# Database Configuration
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/idealab_assessment?schema=public"
PORT=3000
NODE_ENV=development

# Render.com Production Settings
# PORT=10000 (Render requires port 10000)
# NODE_ENV=production

# Database URL Format for Render:
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Example Render Database URL (will be provided by Render):
# DATABASE_URL=postgresql://idealab_user:random_password@idealab-tasks-db.internal.idealab-assessment-api.com:5432/idealab_tasks